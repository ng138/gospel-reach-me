# Gospel Reach Me - Frontend Application
# This is a pure frontend application that runs entirely in the browser

# No environment variables are required for basic functionality
# All Bible verses and data are included in the build

# Optional: For future Worker integration (not currently used)
# VITE_WORKER_URL=https://gospel-reach-me-worker.ng138.workers.dev

# Development mode - leave empty to use local fallbacks
# VITE_WORKER_URL= 